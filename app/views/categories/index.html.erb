<h1>Categories</h1>

<ul>
  <% @categories.each do |c| %>
    <li><%= link_to c.name, category_path(c) %></li>
  <% end %>

  <% @posts.each do |p| %>
  <li><%= link_to p.title, post_path(p) %></li>
  <% end %>

</ul>
