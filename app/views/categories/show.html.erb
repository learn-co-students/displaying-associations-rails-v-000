<h2><%= @category.name %></h2>
<%= @category.posts.count %> post(s):
<ul>
  <% @category.posts.each do |post| %>
  	<h1><%= post.title %></h1>
    <li><%= link_to post.title, post_path(post) %></li>
  <% end %>
</ul>